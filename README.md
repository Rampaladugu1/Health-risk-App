# Backend

Flask API for the XAI mental-health risk assessment prototype.

Required runtime artifacts (not embedded in the notebook): `preprocessor.joblib`, `best_model.joblib`, `xgboost_model.joblib`, and `processed_feature_names.csv`.
